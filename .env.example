# Pygmalion Environment Variables
# Copy this file to .env and fill in your values

# Anthropic API Key (required if not using CLI authentication)
# Get yours at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: Gemini API Key for AI image generation with Imagen 4.0
# Requires:
#   1. pip install -e ".[gemini]"
#   2. Google Cloud billing account enabled
# Get your API key at: https://aistudio.google.com/apikey
# Note: Imagen API requires billing - free tier not available
# GEMINI_API_KEY=...
# Or use: GOOGLE_API_KEY=...

# Optional: Enable Gemini integration (default: false)
# Set to true to enable AI image generation
# PYGMALION_GEMINI_ENABLED=false

# Optional: Default image size for Gemini image generation (default: 1K)
# Options: 1K (~1024px), 2K (~2048px), 4K (~4096px)
# Note: 4K uses Gemini 3 Pro preview model and only supports single image generation
# PYGMALION_GEMINI_IMAGE_SIZE=1K

# Optional: Gemini model for SVG vector graphics generation (default: gemini-2.5-flash)
# Options: gemini-2.5-flash (fast), gemini-3-flash, gemini-3-pro (higher quality)
# PYGMALION_GEMINI_SVG_MODEL=gemini-2.5-flash

# Optional: Default autonomy mode (default: approval)
# Options: approval, auto
# PYGMALION_AUTONOMY_MODE=approval

# ==============================================================================
# Figma MCP Integration (optional)
# ==============================================================================
# Enable Figma integration using the GLips/Figma-Context-MCP server
# Requires: Node.js (npx will auto-install the package)

# Enable Figma integration (true/false)
# PYGMALION_FIGMA_ENABLED=false

# Figma Personal Access Token
# Get from: https://www.figma.com/developers/api#access-tokens
# FIGMA_ACCESS_TOKEN=figd_...

# ==============================================================================
# Grok MCP Integration (optional)
# ==============================================================================
# Enable Grok integration for AI image generation and vision capabilities
# Requires: Grok MCP server (https://github.com/merterbak/Grok-MCP)

# Enable Grok integration (true/false)
# PYGMALION_GROK_ENABLED=false

# xAI API Key
# Get from: https://console.x.ai/
# XAI_API_KEY=xai-...

# Path to your Grok MCP installation
# GROK_MCP_PATH=/path/to/Grok-MCP

